let isDone: boolean = false;

isDone = true;

console.log(typeof(isDone))

let isOk: Boolean = true;

// let isNotOk : boolean = new Boolean(true) Boolean은 object 객체이고 boolean은 primitive type이라 할당 불가능
